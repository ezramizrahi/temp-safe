Check if temperature is safe to take my dog for a walk. Playing around with Rust.

## Run
`cargo run <city_name>` should return a response like:

```bash
Current temperature: 17°C
Temperature is safe.
```
